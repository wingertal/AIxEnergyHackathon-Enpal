[project]
name = "enpal-energy-api"
version = "0.1.0"
description = "Backend for the Enpal home-energy companion app."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"
