# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0

# --- Config & validation ---
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
python-dotenv>=1.0,<2.0

# --- AI layer (chatbot) ---
# OpenAI powers the conversational companion (merged-in chatbot).
openai>=1.3

# --- Conversation persistence (chatbot history) ---
sqlalchemy>=2.0,<3.0

# --- Dev / test ---
# httpx backs Starlette's TestClient used by the test suite.
httpx>=0.28,<1.0
pytest>=8.3,<9.0
pytest-asyncio>=0.25,<1.0
