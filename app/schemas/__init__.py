"""Pydantic request/response models — the API contract for each feature.

These are intentionally light right now. Defining the shapes here lets the
frontend and backend agree on data before the logic is built.
"""
